[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build || echo 'No build script'"]

[start]
cmd = "node psyberherd-apex-implementation.js"

[variables]
NODE_ENV = "production"
NPM_CONFIG_PRODUCTION = "false"